堆的定义：
    堆的特性;
    1、是完全二叉树，即除了最后一层都是满的，最后一层从左到右是满的
    2、通常用数组实现：对应关系：根节点放在1的位置（0索引内容抛弃，为了方便），其他索引按层序遍历放入
    3、当电结点的左子节点为2k，右子节点为2k+1，父结点为【k/2】，这样就可以不用指针而通过索引在树上上下移动
    4、每个结点的键比子结点的键大，而对这两个子节点的顺序没有要求